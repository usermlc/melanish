// Exporting an object that defines the plugins used for CSS processing
export const plugins = {
    tailwindcss: {}, // Tailwind CSS plugin for utility-first styling
    autoprefixer: {}, // Autoprefixer plugin to add vendor prefixes for cross-browser compatibility
};
